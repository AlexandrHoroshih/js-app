{
  "host": "{{HOST}}",
  "backPort": "{{BACK_PORT}}",
  "db": {
    "database": "{{DB_NAME}}",
    "user": "{{DB_USER}}",
    "password": "{{DB_PASSWORD}}",
    "port": "{{DB_PORT}}",
    "host": "{{HOST}}"
  }
}
