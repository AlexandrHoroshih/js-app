{
  "env": "{{NODE_ENV}}",
  "host": "{{HOST}}",
  "backPort": "{{BACK_PORT}}",
  "backUrl": "ws://{{HOST}}:{{BACK_PORT}}",
  "db": {
    "database": "{{DB_NAME}}",
    "user": "{{DB_USER}}",
    "password": "{{DB_PASSWORD}}",
    "port": "{{DB_PORT}}",
    "host": "{{HOST}}"
  },
  "logux": {
    "subprotocol": "{{LOGUX_SUBPROTO}}",
    "supports": "{{LOGUX_SUPPORTS}}"
  }
}
